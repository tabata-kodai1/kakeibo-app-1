# 開発の進め方

このリポジトリでの Issue・ブランチ・PR の運用ルール。[N-33](docs/non-functional.md#開発プロセス品質) に対応する。

## 原則

**1 Issue = 1 ブランチ = 1 PR。**
main への直接 push はしない。すべての変更は PR を経由して取り込む。

## 流れ

1. Issue を立てる（[docs/plan.md](docs/plan.md) のフェーズ表の 1 行 = 1 Issue が目安）
2. main を最新にして、Issue 番号を含むブランチを切る
3. 実装する。API は**受け入れ条件をリクエストスペックに落としてから**実装する
4. ローカルで CI 相当のコマンドを通す（[CLAUDE.md](CLAUDE.md#よく使うコマンド)）
5. PR を作る。本文に `Closes #<Issue番号>` を書く
6. CI が緑になったらマージする（Squash and merge）。ブランチは削除する

## Issue

- 1 Issue は 1 つの PR で閉じられる大きさにする。大きくなったら分割する
- 本文には「やること」と「完了条件」を書く。完了条件は設計書の受け入れ条件を引く
- 種別ごとにテンプレートを用意している（機能実装 / 不具合 / ドキュメント・その他）

## ブランチ

```
feature/<Issue番号>-<概要>
```

例: `feature/12-entries-index-api`

| 接頭辞 | 用途 |
| --- | --- |
| `feature/` | 機能実装 |
| `fix/` | 不具合修正 |
| `docs/` | 設計書・ドキュメントのみの変更 |
| `chore/` | 設定・依存更新など |

## コミットメッセージ

```
<種別>: <何をしたか（日本語・現在形）>
```

種別は `feat` / `fix` / `docs` / `test` / `refactor` / `chore`。
例: `feat: 明細一覧APIを追加する`

## PR

- タイトルは Issue の内容がわかる日本語にする
- 本文に `Closes #<Issue番号>` を書き、マージで Issue が閉じるようにする
- テンプレートのチェック項目をすべて埋めてからレビュー・マージに進む
- 設計書を変更した場合は、**その変更を実装より先に**（同じ PR 内なら差分として明示して）行う（N-18）

## マージの条件

- CI の 4 ジョブ（`scan_ruby` / `lint` / `test` / `frontend`）がすべて緑
- 設計書との差異がない、または設計書側も更新されている

CI は [docs/plan.md](docs/plan.md#フェーズ1-初期化) のフェーズ1 で整備する。
整備が済み次第、main のブランチ保護で 4 ジョブを必須チェックに指定する。
