# 開発の進め方

## 基本の流れ

1 つの Issue に対して、1 つのブランチと 1 つの PR を対応させる。

```
Issue を立てる
  ↓
main を最新にする          git switch main && git pull
  ↓
ブランチを切る              git switch -c feature/5-明細一覧API
  ↓
受け入れ条件をテストに落とす → 実装する
  ↓
コミットする（複数可）
  ↓
push して PR を作る
  ↓
CI が緑になったらマージ
  ↓
ブランチを削除する
```

**main への直接 push はしない**（[N-33](./docs/non-functional.md#開発プロセス品質)）。一人で開発していても、履歴の粒度とレビューのしやすさを保つために PR を通す。

## ブランチ

命名は `<種別>/<Issue番号>-<概要>`。

```
feature/5-明細一覧API
fix/12-一括更新の区分チェック
chore/github-workflow-rules
```

Issue に紐づかない整備作業は、番号を省いてよい。

## コミット

### メッセージ

日本語。1 行目は `<種別>: <何をしたか>` で、体言止めにせず動詞で終える。

```
feat: 月次サマリーAPIを追加する
fix: 一括更新で収支区分の検証が素通りする問題を直す
docs: 設計書を正とする方針を N-18 に明記する
```

| 種別 | 使いどころ |
| --- | --- |
| `feat` | 機能の追加 |
| `fix` | 不具合の修正 |
| `docs` | ドキュメントのみの変更 |
| `refactor` | 挙動を変えない内部の整理 |
| `test` | テストの追加・修正 |
| `chore` | 設定、依存、環境まわり |

### 本文

**「なぜそうしたか」を書く。** 何を変えたかは diff から読み取れるが、判断の理由は書かなければ残らない。

とくに次の場合は本文を厚くする。

- 複数の選択肢があり、片方を選んだとき（採らなかった理由も書く）
- 一見遠回りに見える実装をしたとき
- 設計書を変更したとき（なぜ設計のほうを変えたのか）

## PR

- タイトルはコミットメッセージと同じ規約
- 本文に `Closes #<Issue番号>` を書き、マージ時に Issue が閉じるようにする
- 本文では、コミットの列挙ではなく**何をどう解決したか**を書く
- CI が緑になっていること

## テスト

実装の前に、[features.md](./docs/features.md) の受け入れ条件をリクエストスペックの `it` に落とす。受け入れ条件と `it` が 1 対 1 で対応している状態を保つ（[N-16](./docs/non-functional.md#保守性運用)）。

フロントエンドは手動確認とする。

## 設計書との関係

**`docs/` の設計書を正とする。**

実装中に設計書とのズレを見つけたら、勝手にドキュメントを書き換えない。

| 状況 | 対応 |
| --- | --- |
| 実装が間違っている | 実装を直す |
| 設計を変えるべきだと判断した | **先に設計書を更新する PR** を出し、その後に実装する |

実装を先に変えて設計書を後から追随させることはしない（[N-18](./docs/non-functional.md#保守性運用)）。

## コード品質

コミット前にローカルで通しておく。CI でも実行される。

| 対象 | コマンド |
| --- | --- |
| Ruby の書式 | `bin/rubocop` |
| Ruby の静的セキュリティ解析 | `bin/brakeman` |
| gem の脆弱性 | `bin/bundler-audit` |
| テスト | `bundle exec rspec` |
| フロントの書式・型 | `npm run lint` / `npx vue-tsc --noEmit` |

RuboCop の設定は Rails 同梱の `rubocop-rails-omakase` をそのまま使う。不都合が出た規則だけ `.rubocop.yml` で上書きし、最初から大量の除外を書かない。
