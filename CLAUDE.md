# CLAUDE.md

このリポジトリで作業するときの前提と規約。

## プロジェクト

予算管理型の家計簿 Web アプリ。**「あといくら使えるか」に答えること**が中心的な価値であり、単なる記録の保管庫ではない。機能の取捨選択は、この目的に寄与するかどうかで判断する。

## 最重要ルール: 設計書が正

**`docs/` の設計書を正とする。**

- 実装が設計書と食い違っていたら、**原則として実装を直す**
- 設計そのものを変えると判断したときだけ、**先に設計書を更新してから実装する**
- 実装を先に変えて、設計書を後から追随させることはしない

実装中にズレに気づいても、黙って `docs/` を書き換えない。まず「実装を直すのか、設計を変えるのか」を判断する（[N-18](./docs/non-functional.md#保守性運用)）。

## ドキュメントの役割

迷ったら、該当するドキュメントを読んでから書く。

| ファイル | 内容 |
| --- | --- |
| `docs/requirements.md` | 目的、スコープ、前提条件・制約 |
| `docs/features.md` | F-01〜F-09、API 仕様、バリデーション規則、受け入れ条件 |
| `docs/screens.md` | S-01〜S-04、画面要素 |
| `docs/database.md` | テーブル定義、インデックス、月の絞り込み方法 |
| `docs/non-functional.md` | N-01〜N-40 と各項目の確認方法 |
| `docs/tech-stack.md` | 技術選定の理由、開発環境、設定値の扱い |
| `docs/plan.md` | 6 フェーズの実装計画 |

## 開発フロー

- 作業は GitHub Issue 単位。**1 機能 = 1 Issue = 1 PR**
- ブランチ名は `feature/<Issue番号>-<概要>`
- **main への直接 push はしない**（[N-33](./docs/non-functional.md#開発プロセス品質)）
- PR 本文に `Closes #<Issue番号>` を書く
- 各 PR では、受け入れ条件をそのままリクエストスペックの `it` に落としてから実装する

## コミットメッセージ

日本語で書く。1 行目は `<種別>: <何をしたか>`。

```
docs: 設計書を正とする方針を N-18 に明記する
```

**本文には「なぜそうしたか」を書く。** 何を変えたかは diff を見れば分かるが、なぜその判断をしたかは書かないと残らない。とくに、ある選択肢を採らなかった理由は本文に残す。

種別は `docs` / `feat` / `fix` / `refactor` / `test` / `chore` を使う。

## 実装上の注意

設計時に判明していて、実装で踏みやすい点。

- **`categories` の収支区分の列名は `category_type`。** `type` は ActiveRecord が STI 用に予約しているため避けている
- **`update_all` はバリデーションと `updated_at` の自動更新をスキップする。** 一括更新（F-09）では、件数上限・ID の存在・日付の妥当性・収支区分の一致を、すべてコントローラ側で事前に検証する。単体編集ならモデルバリデーションで済む検証が、一括では素通りする
- **一括削除は `delete_all` を 1 文で使う。** `destroy_all` は件数ぶん DELETE を発行する
- **月の絞り込みは範囲比較で書く。** `WHERE entry_date >= '2026-09-01' AND entry_date < '2026-10-01'`。`EXTRACT` や `TO_CHAR` は列に関数を適用するためインデックスが効かなくなる
- **月次サマリーとカテゴリ別集計は SQL の集計で求める。** アプリ側でループしない（[N-03](./docs/non-functional.md#利用環境性能)）
- **残額の計算に収入は含めない。** `remaining = budget - expense_total`
- **予算 0 円と予算未設定は別の状態。** 未設定は行が存在しない状態で、`budget` と `usage_rate` は `null` を返す
- 金額はすべて整数。タイムゾーンは `Asia/Tokyo`

## 品質

- RuboCop / Brakeman / bundler-audit は Rails 同梱のものをそのまま使う。追加導入しない
- テスト対象は API のリクエストスペック。フロントエンドは手動確認
- CI（`.github/workflows/ci.yml`）は `rails new` の生成物を土台にする

## やらないこと

対象外と決めたものを、気を利かせて追加しない。判断の経緯は `docs/requirements.md` の「対象外」にある。

認証・ログイン / ユーザー管理 / 手動の並び替え（ドラッグ&ドロップ）/ 列ソート / カテゴリ別予算 / グラフ描画 / 前月比較 / カテゴリの CRUD / CSV 入出力 / ページネーション / 複数通貨 / スマートフォン対応
